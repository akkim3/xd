# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Inquisitor' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Inquisitor
	pod 'Firebase'
	pod 'FirebaseCore'
	pod 'FirebaseAuth'
	pod 'FirebaseDatabase'
	pod 'Charts'

  target 'InquisitorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InquisitorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
